<Window x:Class="HaisyaDesktop.View.Account.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctl="clr-namespace:HaisyaDesktop.Control"
        xmlns:local="clr-namespace:HaisyaDesktop.View.Account"
        xmlns:behavior="clr-namespace:HaisyaDesktop.Behavior"
        xmlns:vm="clr-namespace:HaisyaDesktop.ViewModel.Account"
        d:DataContext="{d:DesignInstance Type=vm:LoginViewModel}"
        mc:Ignorable="d"
        Title="ログイン" Height="700" Width="800" Loaded="Window_Loaded"
        WindowStartupLocation="CenterScreen">

  <Window.Resources>
    <Style x:Key="btnStyle" TargetType="Button">
      <Setter Property="Background" Value="Blue"/>
      <Setter Property="Foreground" Value="White" />
      <Setter Property="Height" Value="30" />
      <Setter Property="FontSize" Value="16" />
    </Style>
  </Window.Resources>
  
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition />
      <ColumnDefinition />
    </Grid.ColumnDefinitions>

    <Grid Grid.Column="0" >
      <DockPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <Label DockPanel.Dock="Top" FontSize="34">運送業務システム</Label>
        <Label DockPanel.Dock="Top" Height="20"></Label>
        <StackPanel DockPanel.Dock="Top" Margin="0,5,0,0">
          <Label>会社コード</Label>
          <TextBox x:Name="txtKaisyaCD" Tag="会社コード" Text="{Binding KaisyaCD.Value}"  InputMethod.PreferredImeState="Off" Margin="0,5,0,0"></TextBox>
        </StackPanel>
        <StackPanel DockPanel.Dock="Top" Margin="0,5,0,0">
          <Label>ログインID</Label>
          <TextBox x:Name="txtUserID" Tag="ログインID" Text="{Binding UserID.Value}" InputMethod.PreferredImeState="Off" Margin="0,5,0,0"></TextBox>
        </StackPanel>
        <StackPanel DockPanel.Dock="Top" Margin="0,5,0,0">
          <Label>パスワード</Label>
          <PasswordBox x:Name="txtPass" Tag="パスワード" InputMethod.PreferredImeState="Off" Margin="0,5,0,0" PreviewKeyUp="Password_PreviewKeyUp"></PasswordBox>
        </StackPanel>
        <Button x:Name="btnForgetPass" DockPanel.Dock="Top" Background="Transparent" Margin="0,5,0,0" HorizontalAlignment="Left" Click="btnForgetPass_Click">
          <TextBlock Foreground="Blue" TextDecorations="Underline"  >パスワードをお忘れの方</TextBlock>
        </Button>
        <Button x:Name="btnLoginExec" DockPanel.Dock="Top" Style="{DynamicResource btnStyle}" Margin="0,15,0,0" Content="ログイン" Click="btnLoginExec_Click"></Button>
      </DockPanel>
    </Grid>

    <Grid Grid.Column="1" Background="Cyan">
      
    </Grid>

  </Grid>


</Window>
