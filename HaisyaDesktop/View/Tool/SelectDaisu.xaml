<Window x:Class="HaisyaDesktop.View.Tool.SelectDaisu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HaisyaDesktop.View.Tool"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowState="Normal"
        WindowStyle="None"
        Title="台数選択" Height="450" Width="200" Loaded="Window_Loaded">

  <Window.Resources>
    
  </Window.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="30" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <DockPanel Grid.Row="0" Background="Azure">
      <Button Name="btnClose" Content="閉じる" DockPanel.Dock="Right" Width="100" Click="btnClose_Click"/>
      <Label Content="台数選択" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="0" />
    </DockPanel>

    <Border Grid.Row="0" BorderBrush="DarkGray" BorderThickness="2" />
    <Border Grid.Row="1" BorderBrush="DarkGray" BorderThickness="2" />
    <Grid Grid.Row="1" >
      <ListBox x:Name="lstDaisu" ItemsSource="{Binding DaisuList}" SelectionChanged="ListBox_SelectionChanged" >
        <ListBox.Template>
          <ControlTemplate TargetType="ItemsControl">
            <Border BorderThickness="0" BorderBrush="Transparent" Background="Transparent" CornerRadius="10" >
              <ItemsPresenter Margin="5">
              </ItemsPresenter>
            </Border>
          </ControlTemplate>
        </ListBox.Template>

        <ListBox.ItemTemplate>
          <DataTemplate>
            <Border Background="Silver" CornerRadius="5" Height="30" Margin="3" BorderBrush="Black" BorderThickness="3">
              <TextBlock Text="{Binding}" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center" />
            </Border>
            
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>
    </Grid>
  </Grid>
</Window>
