<Window x:Class="HaisyaDesktop.View.Tool.MailSend"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HaisyaDesktop.View.Tool"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowState="Normal"
        WindowStyle="ToolWindow"
        Title="メッセージ" MinHeight="450" MinWidth="500">
  <Window.Resources>
    <Style x:Key="MailButton" TargetType="Button">
      
    </Style>
  </Window.Resources>

  <Grid Background="LightGray">
  <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="10" Margin="10" Background="White" >
  <Grid x:Name="Main">
    <Grid.RowDefinitions>
      <RowDefinition Height="100" />
      <RowDefinition Height="50" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>


    <Border Grid.Row="0" BorderBrush="Gray" BorderThickness="0,0,0,1">
    <Grid >
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="80" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <Button Grid.Column="0" Content="送信" Margin="10" />

      <Grid Grid.Column="1">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="80" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" >
          <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
          </Grid.RowDefinitions>

          <Button Grid.Row="0" Content="宛先" Margin="5" Style="{StaticResource MailButton}" />
          <Button Grid.Row="1" Content="ＣＣ" Margin="5" Style="{StaticResource MailButton}" />
          <TextBlock Grid.Row="2" Text="件名" Margin="5" VerticalAlignment="Center" HorizontalAlignment="Center"  />

        </Grid>

        <Grid Grid.Column="1" >
          <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
          </Grid.RowDefinitions>

          <Border Grid.Row="0" BorderBrush="Gray" BorderThickness="0,0,0,1" Margin="1">
            <TextBlock Text=""  VerticalAlignment="Center" HorizontalAlignment="Left"  />
          </Border>
          <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="0,0,0,1" Margin="1">
            <TextBlock Text=""  VerticalAlignment="Center" HorizontalAlignment="Left"  />
          </Border>

          <TextBlock Grid.Row="2" Text=""  VerticalAlignment="Center" HorizontalAlignment="Left"  />

         </Grid>
       

      </Grid>
      
    </Grid>
    </Border>


    <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="0,0,0,1">
           
    </Border>

        <Grid Grid.Row="2">
          <RichTextBox AcceptsTab="True" />
        </Grid>
    
  </Grid>

  </Border>
  </Grid>
</Window>
