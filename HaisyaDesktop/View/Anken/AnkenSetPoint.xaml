<Window x:Class="HaisyaDesktop.View.Anken.AnkenSetPoint"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Wpf="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf" 
        xmlns:local="clr-namespace:HaisyaDesktop.View.Anken"
        mc:Ignorable="d"
        WindowStyle="ThreeDBorderWindow"
        Title="案件ルート設定" MinHeight="450" MinWidth="800" Loaded="Window_Loaded">
  
  <Window.Resources>
    <Style x:Key="ComboBoxTextBoxStyle" TargetType="{x:Type TextBox}">
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type TextBox}">
            <Grid>
              <Border Name="ComboBoxTextBoxStyleBorder" CornerRadius="2" 
                BorderThickness="0,1,0,1"  Margin="0,0,1,1"   Background="{TemplateBinding Background}">
                <Border.Style>
                  <Style TargetType="Border">
                    <Style.Triggers>
                      <Trigger Property="IsMouseOver" Value="true">
                        <Setter Property="Background" Value="White"/>
                      </Trigger>
                    </Style.Triggers>
                  </Style>
                </Border.Style>
                <ScrollViewer x:Name="PART_ContentHost"/>
              </Border>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Setter Property="IsReadOnly" Value="True"/>
    </Style>

    <!--Rounded corners combo box-->
    <Style x:Key="roundedCornersComboBox" TargetType="{x:Type ComboBox}" >
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type ComboBox}">
            <Border Name="roundedCornerComboBoxBorder" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="3" ClipToBounds="True" Background="White">
              <Border.Style>
                <Style TargetType="Border">
                  <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="true">
                      <Setter Property="Background" Value="White"/>
                    </Trigger>
                  </Style.Triggers>
                </Style>
              </Border.Style>
              <Canvas>
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                  </Grid.ColumnDefinitions>
                  <TextBox Name="PART_EditableTextBox"  Panel.ZIndex="0" 
                    Style="{StaticResource ComboBoxTextBoxStyle}"  Padding="0,0,0,0" 
                    IsHitTestVisible="False"  Height="{TemplateBinding Height}">
                  </TextBox>
                  
                  <ToggleButton Grid.Column="1" Margin="0, 0, 0, 0" 
                     Panel.ZIndex="1" 
                     HorizontalAlignment="Center" 
                     VerticalAlignment="Center" 
                     BorderBrush="Transparent" 
                     Background="Transparent" 
                     Height="{TemplateBinding Height}" 
                     Width="60" 
                     IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                     HorizontalContentAlignment="Right" 
                     ClickMode="Press">

                    <ToggleButton.Style>
                      <Style TargetType="ToggleButton">
                        <Style.Triggers>
                          <Trigger Property="IsMouseOver"  Value="true">
                            <Setter Property="Background"  Value="White" />
                          </Trigger>
                        </Style.Triggers>
                      </Style>
                    </ToggleButton.Style>

                    <Path Grid.Column="1" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center" 
                       Data="M 0 0 L 4 4 L 8 0 Z" 
                       Fill="DodgerBlue" />
                  </ToggleButton>
                  <ContentPresenter Name="ContentSite" 
                     Cursor="Arrow" 
                    Content="{TemplateBinding SelectionBoxItem}" 
                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" 
                    ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Left" 
                    Margin="3,0,0,0">
                  </ContentPresenter>
                  <Popup Name="Popup" 
                    Placement="Bottom" 
                    IsOpen="{TemplateBinding IsDropDownOpen}" 
                    AllowsTransparency="True" 
                    Focusable="False" 
                    PopupAnimation="Slide">
                    <Grid Name="DropDown" 
                       SnapsToDevicePixels="True"     
                       MinWidth="{TemplateBinding ActualWidth}" 
                       MaxHeight="{TemplateBinding MaxDropDownHeight}">
                      <Border 
                          x:Name="DropDownBorder" 
                          BorderThickness="1" 
                          CornerRadius="5" 
                          Background="White" 
                          BorderBrush="Black"/>
                      <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                      </ScrollViewer>
                    </Grid>
                  </Popup>
                </Grid>
              </Canvas>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>
  
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition x:Name="Head" Height="50" />
      <RowDefinition />
    </Grid.RowDefinitions>
    <Grid Grid.Row="0">
      <Grid.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
          <GradientStop Color="DarkBlue"/>
          <GradientStop Color="LightBlue" Offset="1"/>
        </LinearGradientBrush>
      </Grid.Background>

      <Grid.ColumnDefinitions>
        <ColumnDefinition />
        <ColumnDefinition />
        <ColumnDefinition />
        <ColumnDefinition />
        <ColumnDefinition />
        <ColumnDefinition />
      </Grid.ColumnDefinitions>

      <Button Name="btnMenu" Grid.Column="0" Content="閉じる" Style="{StaticResource MenuButtonCommonStyle}" Click="btnMenu_Click" />
      <Button Name="btnMenu1" Grid.Column="1" Content="" Style="{StaticResource MenuButtonCommonStyle}" />
      <Button Name="btnMenu2" Grid.Column="2" Content="" Style="{StaticResource MenuButtonCommonStyle}" />
      <Button Name="btnMenu3" Grid.Column="3" Content="" Style="{StaticResource MenuButtonCommonStyle}" />
      <Button Name="btnMenu4" Grid.Column="4" Content="" Style="{StaticResource MenuButtonCommonStyle}" />
      <Button Name="btnMenu5" Grid.Column="5" Content="" Style="{StaticResource MenuButtonCommonStyle}" />
    </Grid>

    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="4*" />
        <ColumnDefinition Width="15" />
        <ColumnDefinition Width="6*" />
      </Grid.ColumnDefinitions>

      <Border Grid.Column="1" BorderBrush="Black" BorderThickness="2" >
        <GridSplitter HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="LightBlue" />
      </Border>
      

      <Grid Grid.Column="0">
        <DockPanel>

          <Grid DockPanel.Dock="Top" Background="Blue">
            <DockPanel >

              

              <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Width="200" VerticalAlignment="Center">
                <Button x:Name="btnTsumiPlus" DockPanel.Dock="Right" Content="経由地を追加" Width="auto" Margin="0,5" FontSize="16"  Style="{StaticResource DefaultButtonStyle}"></Button>
              </StackPanel >

              <Grid DockPanel.Dock="Top" >
                <DockPanel>
                  <TextBlock DockPanel.Dock="Bottom" Text="{Binding TollDisplay}" Height="20" Margin="0" />
                  
                  <ComboBox DockPanel.Dock="Right" Width="100" Margin="5" FontSize="16" VerticalContentAlignment="Center" >
                    
                    <ComboBoxItem Content="全て" Tag="ALL"  IsSelected="True" />
                    <ComboBoxItem Content="一般優先" Tag="ALL"  />
                    <ComboBoxItem Content="高速優先" Tag="ALL"  />
                    <ComboBoxItem Content="高速指定" Tag="ALL"  />
                  </ComboBox>
                  <Border Height="40" Margin="5" CornerRadius="5" Background="White" >
                    <DockPanel>
                      <TextBlock DockPanel.Dock="Left" Text="出&#10;発" TextWrapping="Wrap" Width="20" TextAlignment="Center" FontSize="14" Foreground="DarkBlue"  />
                      <Button DockPanel.Dock="Right" Content="×" Background="Transparent" Width="20" />
                      <TextBox DockPanel.Dock="Bottom" FontSize="14" Height="80" />
                      
                    </DockPanel>
                  </Border>
                </DockPanel>
                
              </Grid>

              <DockPanel DockPanel.Dock="Bottom" >
                <Border Height="40" Margin="5,5,130,15" CornerRadius="5" Background="White">
                  <DockPanel>
                    <TextBlock DockPanel.Dock="Left" Text="到&#10;着" TextWrapping="Wrap" Width="20" TextAlignment="Center" FontSize="14" Foreground="Red" />
                    <Button DockPanel.Dock="Right" Content="×" Background="Transparent" Width="20" />
                    <TextBox DockPanel.Dock="Bottom" />
                  </DockPanel>
                </Border>

              </DockPanel>


              <ListBox x:Name="list"  DockPanel.Dock="Top" ItemsSource="{Binding PointList, NotifyOnSourceUpdated=True}" 
                                            ScrollViewer.HorizontalScrollBarVisibility="Hidden" ScrollViewer.VerticalScrollBarVisibility="Hidden" 
                                            Margin="0,-5,0,0" VerticalContentAlignment="Center" FontSize="12" Background="Transparent">

                <ListBox.Template>
                  <ControlTemplate TargetType="ItemsControl">
                    <ItemsPresenter Margin="3,5,5,5">
                    </ItemsPresenter>
                  </ControlTemplate>
                </ListBox.Template>
                
                <ListBox.ItemTemplate>
                  <DataTemplate>
                    <DockPanel Background="Transparent" Margin="0,5,0,0">

                      <TextBlock DockPanel.Dock="Bottom" Text="{Binding TollDisplay}" Height="20" Margin="0" />
                      
                      <Button x:Name="btnTsumiDel" DockPanel.Dock="Left" Content="－" Width="30"  Style="{StaticResource DefaultButtonStyle}"></Button>


                      <ComboBox x:Name="ABC"  DockPanel.Dock="Right" Width="80" Margin="5,0,0,0" FontSize="16" VerticalContentAlignment="Center" SelectionChanged="ComboBox_SelectionChanged" ItemsSource="{Binding aa}"  >
                        <ComboBoxItem Content="全て" Tag="ALL"  />
                        <ComboBoxItem Content="一般優先" Tag="ALL"  />
                        <ComboBoxItem Content="高速優先" Tag="ALL"  />
                        <ComboBoxItem Content="高速指定" Tag="ALL"  />
                      </ComboBox>

                      

                      <Border Height="40" CornerRadius="5" Background="White" Margin="5,0,0,0">
                        <DockPanel>
                          <TextBlock DockPanel.Dock="Left" Text="経由" TextWrapping="Wrap" Width="20" TextAlignment="Center" FontSize="14" Foreground="Green" />
                          <Button DockPanel.Dock="Right" Content="×" Background="Transparent" Width="20" />
                          <TextBox DockPanel.Dock="Bottom" />
                          
                        </DockPanel>
                      </Border>
                      
                    </DockPanel>
                  </DataTemplate>
                </ListBox.ItemTemplate>
                
                
              </ListBox>
 
            </DockPanel>
          </Grid>

          <Grid DockPanel.Dock="Top">
            
          </Grid>
          
        </DockPanel>
        
      </Grid>

      <Grid Grid.Column="2">
        <Wpf:WebView2 x:Name="webView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  />
      </Grid>
     
    </Grid>
  </Grid>
</Window>
