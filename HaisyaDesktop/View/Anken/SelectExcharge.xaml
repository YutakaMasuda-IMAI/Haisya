<Window x:Class="HaisyaDesktop.View.Anken.SelectExcharge"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HaisyaDesktop.View.Anken"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowState="Normal"
        WindowStyle="None"
        Title="追加料金項目選択" Height="450" Width="500" Loaded="Window_Loaded">

  <Window.Resources>

  </Window.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="30" />
      <RowDefinition Height="50" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <DockPanel Grid.Row="0" Background="Azure">
      <Button Name="btnClose" Content="閉じる" DockPanel.Dock="Right" Width="100" Click="btnClose_Click"/>
      <Label Content="台数選択" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="0" />
    </DockPanel>

    <Grid Grid.Row="1" Background="Azure">
      <StackPanel Orientation="Vertical">
        <Label Foreground="Red" FontWeight="Bold"></Label>
        <Label Foreground="Red" FontWeight="Bold"></Label>
      </StackPanel>
    </Grid>

    <Border Grid.Row="1" BorderBrush="DarkGray" BorderThickness="2" />
    <Border Grid.Row="2" BorderBrush="DarkGray" BorderThickness="2" />
    <Grid Grid.Row="2" >
      <ListBox x:Name="lst" ItemsSource="{Binding AnkenExchargeList}"  SelectionChanged="ListBox_SelectionChanged" >
        <ListBox.Template>
          <ControlTemplate TargetType="ItemsControl">
            <Border BorderThickness="0" BorderBrush="Transparent" Background="Transparent" CornerRadius="10" >
              <ItemsPresenter Margin="5">
              </ItemsPresenter>
            </Border>
          </ControlTemplate>
        </ListBox.Template>

        <ListBox.ItemTemplate>
          <DataTemplate>
            <Border Background="LemonChiffon" CornerRadius="5" Height="30" Margin="3" BorderBrush="Black" BorderThickness="3">
              <Grid Margin="5,0">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="0.5*" />
                  <ColumnDefinition Width="2*" />
                  <ColumnDefinition Width="1.2*" />
                  <ColumnDefinition Width="1.2*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding SIZE}" VerticalAlignment="Center" HorizontalAlignment="Center" />
                <TextBlock Grid.Column="1" Text="{Binding Komoku_Key}" VerticalAlignment="Center" HorizontalAlignment="Center" />
                <TextBlock Grid.Column="2" Text="{Binding Komoku_Name}" VerticalAlignment="Center" HorizontalAlignment="Left" />
                <StackPanel Grid.Column="3" Orientation="Horizontal">
                  <Label VerticalContentAlignment="Center">標準:</Label>
                  <TextBlock Text="{Binding StdExcharge, StringFormat={}{0:N0}, ConverterCulture=ja-JP}" VerticalAlignment="Center" HorizontalAlignment="Left" />
                </StackPanel>
                <StackPanel Grid.Column="4" Orientation="Horizontal">
                  <Label VerticalContentAlignment="Center">原価:</Label>
                  <TextBlock Text="{Binding GrossExcharge, StringFormat={}{0:N0}, ConverterCulture=ja-JP}" VerticalAlignment="Center" HorizontalAlignment="Left" />
                </StackPanel>
              </Grid>
            </Border>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>
    </Grid>
  </Grid>
</Window>