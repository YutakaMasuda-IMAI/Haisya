<Window x:Class="HaisyaDesktop.View.Haisya.SyabanRenraku"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HaisyaDesktop.View.Haisya"
        xmlns:control="clr-namespace:HaisyaDesktop.Control"
        mc:Ignorable="d"
        WindowStyle="ThreeDBorderWindow"
        Title="車番連絡" MinHeight="450" MinWidth="800" Loaded="Window_Loaded">

  <Window.Resources>

    <Style x:Key="ColumnHeaderStyle1" TargetType="DataGridColumnHeader">
      <Setter Property="Height" Value="30"/>
      <Setter Property="Background" Value="LightBlue"/>
      <Setter Property="Foreground" Value="Black"/>
      <Setter Property="FontSize" Value="20" />
      <Setter Property="Margin" Value="10,0,10,0"/>
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="ToolTip" Value="Click to sort."/>
        </Trigger>
      </Style.Triggers>
    </Style>
  </Window.Resources>


  <Grid x:Name="MainGrid">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition x:Name="Head" Height="50" />
        <RowDefinition Height="*" />
        <RowDefinition x:Name="Tail" Height="50" />
      </Grid.RowDefinitions>

      <Grid Grid.Row="0">
        <Grid.Background>
          <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="Black"/>
            <GradientStop Color="LightGray" Offset="1"/>
          </LinearGradientBrush>
        </Grid.Background>

        <Grid.ColumnDefinitions>
          <ColumnDefinition />
          <ColumnDefinition />
          <ColumnDefinition />
          <ColumnDefinition />
          <ColumnDefinition />
          <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Button Name="btnTopMenu" Grid.Column="0" Content="メニュー" Style="{StaticResource MenuButtonCommonStyle}" Click="btnMenu_Click" />
        <Button Name="btnTopMenu1" Grid.Column="1" Content="日付変更" Style="{StaticResource MenuButtonCommonStyle}" />
        <Button Name="btnTopMenu2" Grid.Column="2" Content="" Style="{StaticResource MenuButtonCommonStyle}" />
        <Button Name="btnTopMenu3" Grid.Column="3" Content="" Style="{StaticResource MenuButtonCommonStyle}" />
        <Button Name="btnTopMenu4" Grid.Column="4" Content="" Style="{StaticResource MenuButtonCommonStyle}" />
        <Button Name="btnTopMenu5" Grid.Column="5" Content="" Style="{StaticResource MenuButtonCommonStyle}" />
      </Grid>

      <Grid Grid.Row="2">
        <Grid.Background>
          <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="Black" Offset="1"/>
            <GradientStop Color="LightGray" Offset="0"/>
          </LinearGradientBrush>
        </Grid.Background>
        <Grid.ColumnDefinitions>
          <ColumnDefinition />
          <ColumnDefinition />
          <ColumnDefinition />
          <ColumnDefinition />
          <ColumnDefinition />
          <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Button x:Name="btnUnder0" Grid.Column="0" Content="一括車番連絡" Style="{StaticResource MenuButtonCommonStyle}" />
        <Button x:Name="btnUnder1" Grid.Column="1" Content="" Style="{StaticResource MenuButtonCommonStyle}" />
        <Button x:Name="btnUnder2" Grid.Column="2" Content="" Style="{StaticResource MenuButtonCommonStyle}" />
        <Button x:Name="btnUnder3" Grid.Column="3" Content="" Style="{StaticResource MenuButtonCommonStyle}" />
        <Button x:Name="btnUnder4" Grid.Column="4" Content="" Style="{StaticResource MenuButtonCommonStyle}" />
        <Button x:Name="btnUnder5" Grid.Column="5" Content="" Style="{StaticResource MenuButtonCommonStyle}" />
      </Grid>

      <Grid Grid.Row="1">
        <Grid.RowDefinitions>
          <RowDefinition Height="40"/>
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="1">
          <DataGrid x:Name="dgKokyakuList" ItemsSource="{Binding SyabanRenrakuList}" AutoGenerateColumns="False" CanUserAddRows="False" 
                    ColumnHeaderStyle="{StaticResource ColumnHeaderStyle1}">
            <DataGrid.RowStyle>
              <Style TargetType="DataGridRow">
                <Setter Property="Height" Value="40"/>
                <!--<Setter Property="Background" Value="{Binding RowColor}"/>-->
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="Background" Value="White"/>
              </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>
              <control:DataGridCheckBoxColumnWithAlignment Header="選択" Binding="{Binding SelectKubun}" />
              <control:DataGridTextColumnWithAlignment Header="ステータス" Binding="{Binding Status}" HorizontalAlignment="Center" FontSize="20"  />
              
              <DataGridTemplateColumn Header="プレビュー">
                <DataGridTemplateColumn.CellTemplate>
                  <DataTemplate>
                    <Button Name="btnPreview" Width="100" Content="プレビュー" Click="btnPreview_Click" Background="DeepSkyBlue" />
                  </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
              </DataGridTemplateColumn>
              
              <DataGridTemplateColumn Header="案件">
                <DataGridTemplateColumn.CellTemplate>
                  <DataTemplate>
                    <Button Width="100" Content="案件詳細" Background="Silver" />
                  </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
              </DataGridTemplateColumn>

              <control:DataGridTextColumnWithAlignment Header="荷主" Binding="{Binding KokyakuName}" FontSize="16" />
              <control:DataGridTextColumnWithAlignment Header="担当者" Binding="{Binding KokyakuTantouName}" FontSize="16" />
              <control:DataGridTextColumnWithAlignment Header="電話番号" Binding="{Binding KokyakuPhoneNumber}" FontSize="16" />
              <control:DataGridTextColumnWithAlignment Header="希望連絡期日" Binding="{Binding NumberCommLimitDateDisplay}" FontSize="16" />
              <control:DataGridTextColumnWithAlignment Header="案件数" Binding="{Binding AnkenCount}" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" />
              <control:DataGridTextColumnWithAlignment Header="確定配車" Binding="{Binding AnkenCommitCount}" FontSize="22" HorizontalAlignment="Center"  />
              <!--<control:DataGridTextColumnWithAlignment Header="暫定配車" Binding="{Binding AnkenTempCount}" FontSize="20" HorizontalAlignment="Center" />-->
              <DataGridTemplateColumn Header="暫定配車" IsReadOnly="True">
                <DataGridTemplateColumn.CellTemplate>
                  <DataTemplate>
                    <DockPanel >
                      <TextBlock Text="{Binding AnkenTempCount}" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="0" FontSize="20" />
                      <DockPanel.Style>
                        <Style TargetType="DockPanel">
                          <Style.Triggers>
                            <DataTrigger Binding="{Binding AnkenTempCountFlg}" Value="1">
                              <Setter Property="Background" Value="Yellow" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding AnkenTempCountFlg}" Value="0">
                              <Setter Property="Background" Value="Transparent" />
                            </DataTrigger>
                          </Style.Triggers>
                        </Style>
                      </DockPanel.Style>
                    </DockPanel>
                  </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
              </DataGridTemplateColumn>
              
              <!--<control:DataGridTextColumnWithAlignment Header="未配車" Binding="{Binding AnkenNonCount}" FontSize="20" HorizontalAlignment="Center" />-->
              <DataGridTemplateColumn Header="未配車" IsReadOnly="True">
                <DataGridTemplateColumn.CellTemplate>
                  <DataTemplate>
                    <DockPanel >
                      <TextBlock Text="{Binding AnkenNonCount}" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="0" FontSize="20" />
                      <DockPanel.Style>
                        <Style TargetType="DockPanel">
                          <Style.Triggers>
                            <DataTrigger Binding="{Binding AnkenNonCountFlg}" Value="1">
                              <Setter Property="Background" Value="Yellow" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding AnkenNonCountFlg}" Value="0">
                              <Setter Property="Background" Value="Transparent" />
                            </DataTrigger>
                          </Style.Triggers>
                        </Style>
                      </DockPanel.Style>
                    </DockPanel>
                  </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
              </DataGridTemplateColumn>


            </DataGrid.Columns>

            <DataGrid.GroupStyle>
              <GroupStyle>
                <GroupStyle.ContainerStyle>
                  <Style TargetType="{x:Type GroupItem}">
                    <Setter Property="Margin" Value="0,0,0,10"/>
                    <Setter Property="Template">
                      <Setter.Value>
                        <ControlTemplate TargetType="{x:Type GroupItem}">
                          <Expander IsExpanded="True" Background="SlateBlue" Foreground="White">
                            <Expander.Header>
                              <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}" Margin="5,0,5,0"/>
                                <StackPanel Orientation="Horizontal">
                                  <TextBlock Text="("/>
                                  <TextBlock Text="{Binding ItemCount}"/>
                                  <TextBlock Text=")"/>
                                </StackPanel>
                              </StackPanel>
                            </Expander.Header>
                            <Expander.Content>
                              <ItemsPresenter />
                            </Expander.Content>
                          </Expander>
                        </ControlTemplate>
                      </Setter.Value>
                    </Setter>
                  </Style>
                </GroupStyle.ContainerStyle>
              </GroupStyle>
            </DataGrid.GroupStyle>
          </DataGrid>
        </Grid>

        <Grid Grid.Row="0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>

          <TextBlock Grid.Column="0" Text="2022年6月23日(木曜日)" VerticalAlignment="Center" FontSize="20" />

          <StackPanel Orientation="Horizontal" Grid.Column="1" Margin="2" >
            <TextBlock Text="表示グループ：" VerticalAlignment="Center" FontSize="18" />
            <ComboBox VerticalAlignment="Center" FontSize="18" >
              <ComboBoxItem Content="大型ウィング" IsSelected="True" />
              <ComboBoxItem Content="〇〇営業所" />
            </ComboBox>
          </StackPanel>

          <StackPanel Orientation="Horizontal" Grid.Column="4" Margin="0" >
            <TextBlock Text="グループ化：" VerticalAlignment="Center" FontSize="18" />
            <Border BorderBrush="Black" BorderThickness="1" CornerRadius="2" Padding="0" Margin="1,0,2,0" Background="Transparent">
              <StackPanel Orientation="Horizontal" Margin="0" VerticalAlignment="Center">
                <RadioButton Content="ステータス" GroupName="grouping" Margin="5,2,8,0" IsChecked="True" Checked="OnGroupingByAnkenStatus" FontSize="16" VerticalContentAlignment="Center" />
                <RadioButton Content="なし" GroupName="grouping" Margin="20,2,8,0" Checked="OnGroupingNone" FontSize="16" VerticalContentAlignment="Center" />
              </StackPanel>
            </Border>
          </StackPanel>
        </Grid>

      </Grid>
      
      
    </Grid>
  </Grid>
</Window>
